<!DOCTYPE html>
<html>

<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>DaBakery</title>
  <link rel="stylesheet" type="text/css"
            href="https://fonts.googleapis.com/css?family=Marcellus">
  <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css"/>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

</head>
<body>

  <div class="wrap">
      <header class="main-header">
        <div class="container">

          <h1="name">Your Favorite Neighborhood Bakery</h1>
            <div class="pull-right user-info">

            </div>
              <ul class="main-nav">
                <li><%=link_to 'Home', root_path %></li>
                <li><a href="#home" id="#main-nav"></a></li>
                <li><a href="#contact" id="#main-nav">Contact & Locations</a></li>
                <li><%=link_to 'Our Products', items_path %></li>
                <li><% if current_user %>
                        <div class="pull-right">
                          <%= link_to 'My Cart', cart_path %>
                          <i class="fi-shopping-cart"></i>
                          (<span class="cart-count"><%=current_user.cart_count%></span>)
                        </div>
                    <% else %>


                    <% end %>
                </li>
                <li><% if current_user %>
                  <div class="pull-right">
                    <%= current_user.email %> <%= link_to "Sign Out", session_path(current_user), method: :delete %>
                  </div>
                <% else %>
                    <%= link_to "Sign In", new_session_path %> or
                    <%= link_to "Sign Up", new_user_path %>
                  <% end %>
                </li>
              </ul>

            </div>
      </header>


          <%= yield %>
  </div>
</body>

<footer class="main-footer">
  <span>&copy;2016 Hillary Feller</span>
</footer>
</html>
